//make an array of numbers
var gainNumbers = [25, 42, 46, 53];
//double each number
var doubleNumbers = gainNumbers.map(function (gainNumbers) { return gainNumbers * 2; });
//new double array
console.log(doubleNumbers);
